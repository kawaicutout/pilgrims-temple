# binaries and local run data (rebuilt by make; never commit)
/pilgrims-temple
/pilgrims-temple.exe
/terminal
/wasm
pilgrims-temple-web.zip
bin/
scores.json
dist/
/tmp/
*.tmp

# web build artifacts (rebuilt by make web; Pages action rebuilds them)
web/main.wasm
web/main.wasm.br
web/wasm_exec.js

# OS
.DS_Store
Thumbs.db

# Go
vendor/
# local review docs (never commit or push; see localdoc/README.md)
/localdoc/

# Pages workflow needs workflow-scope auth; add back with git add -f
.github/workflows/pages.yml
